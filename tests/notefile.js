npx playwright codegen https://www.google.com lệnh tạo màn hình UI để check lệnh


// tạo lệnh push file lên github
git checkout -b my-branch   # Tạo và chuyển sang nhánh mới (hoặc `git checkout my-branch` nếu đã có)
git add .                   # Thêm tất cả file vào Git
git commit -m "Thay đổi mới" # Commit thay đổi với thông điệp
git push --set-upstream origin my-branch  # Đẩy nhánh lên remote